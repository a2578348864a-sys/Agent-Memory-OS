# 每日防错索引

记录高频易错检查点，供 Agent 开工时对照自查。

- [ ] `.cmd` / `.bat` 是否保持纯 ASCII（中文说明移到 `.md`/`.ps1`，不嵌在批处理里）？
- [ ] 含中文的 PowerShell 5.1 脚本是否采用兼容编码（UTF-8 带 BOM 保存、显式 `-Encoding UTF8` 读写）？
- [ ] 写任务结束时是否按规则执行快照备份（`backup-obsidian-vault.ps1`）？
